// Boost cost/duration. Kept in a plain module (no "use server"/"server-only")
// so both the server action and client components can import it.
export const BOOST_COST = 30;
export const BOOST_DAYS = 7;
